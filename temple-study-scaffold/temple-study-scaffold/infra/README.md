# Deployment files owned by member 4

Add a reviewed container or systemd setup here after the team chooses an Azure Linux VM and domain. It should run the web server and reminder worker against one persistent local SQLite file, configure HTTPS and restart policies, and reference secrets stored outside Git. See `docs/DEPLOYMENT.md`. No deployment is included in this scaffold.
