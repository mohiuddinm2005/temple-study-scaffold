# Reminder worker owned by member 4

`index.ts` is a placeholder. Once scheduling is implemented, add a run script, clock-injected tests, lease/retry behavior, delivery logs without sensitive endpoints, and process supervision. The web process and worker must share the same local SQLite database on one host.
